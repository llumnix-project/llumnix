apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 使用 namespace 隔离，不需要 namePrefix
# 每个 namespace 中都使用相同的资源名称

resources:
  - decode.yaml
  - prefill.yaml
  - serviceAccount.yaml
  - redis.yaml
  - gateway.yaml
  - scheduler.yaml
