[build-system]
requires = ["setuptools>=64", "setuptools_scm", "pip>=24.0", "grpcio-tools>=1.59.5,<=1.62.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
# no extra settings needed, presence enables setuptools-scm
